import XCTest
@testable import MyComposablePackage

final class MyComposablePackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MyComposablePackage().text, "Hello, World!")
    }
}
